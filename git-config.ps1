Write-Host "Configurando GIT" -BackgroundColor Blue
git config --global user.name "AdminAAA"
git config --global user.email "mail@mail.com"
git config --global color.ui true
git config --global alias.nicelog "log --oneline --graph --all" (git nicelog)